"""cli-proxy-logger (Python learning port).

Local reverse proxy that intercepts and logs Claude Code / Codex CLI requests
(request / response / tool calls). Mirrors the Node.js reference implementation.
"""

__version__ = "0.1.0"
